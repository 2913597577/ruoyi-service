<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.caseDetail.mapper.DcCaseTrackingMapper">

    <select id="selectTodayFollowUpByLegalSupport" resultType="map">
        SELECT *
        FROM dc_case_tracking
        WHERE del_flag = 0
        AND next_tracking_time >= CURDATE()
        AND next_tracking_time &lt; DATE_ADD(CURDATE(), INTERVAL 1 DAY)
        <if test="legalSupportId != null">
            AND legal_support_id = #{legalSupportId}
        </if>
    </select>


    <select id="selectMonthlyCaseTracking" resultType="map">
        SELECT *
        FROM dc_case_tracking
        WHERE del_flag = 0
        AND tracking_time BETWEEN DATE_FORMAT(CURDATE(), '%Y-%m-01')
        AND LAST_DAY(CURDATE())
        <if test="legalSupportId != null">
            AND legal_support_id = #{legalSupportId}
        </if>
    </select>
</mapper>
