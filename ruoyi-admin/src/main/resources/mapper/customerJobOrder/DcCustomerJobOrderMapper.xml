<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.legalSupport.mapper.DcCustomerJobOrderMapper">

    <select id="selectMonthlyJobOrder" resultType="map">
        SELECT *
        FROM dc_customer_job_order
        WHERE del_flag = 0
        AND create_time BETWEEN DATE_FORMAT(CURDATE(), '%Y-%m-01')
        AND LAST_DAY(CURDATE())
        <if test="legalSupportId != null">
            AND legal_support_id = #{legalSupportId}
        </if>
    </select>

</mapper>
